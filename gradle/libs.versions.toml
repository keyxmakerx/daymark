[versions]
# AGP 8.13 is the LAST release on the 8.x line. It needs Gradle >= 8.13 and JDK 17 (both satisfied),
# and unlike AGP 9 it keeps the legacy variant API and does not turn on built-in Kotlin — so this is
# currency without the AGP 9 DSL migration. Going past it means AGP 9, which drags in Gradle 9.5+,
# Kotlin >= 2.2.10 and Hilt >= 2.59 all at once. See docs/TOOLCHAIN.md.
agp = "8.13.0"
kotlin = "2.0.21"
ksp = "2.0.21-1.0.28"
# core / activity / lifecycle / androidx.hilt / navigation are deliberately held BELOW their latest.
# 1.19.0 / 1.13.0 / 2.11.0 / 1.4.0 / 2.9.x each require **AGP >= 9.1 and compileSdk >= 37**; bumping
# them turned main red via CheckAarMetadata. They do NOT move with an AGP 9 migration: they need
# compileSdk 37 -> AGP 9.2 -> Kotlin > 2.3.21, and Dagger caps Kotlin at 2.3.21. Gated upstream.
# Note androidx.hilt is NOT the Dagger 2.x line and versions separately, which makes it look
# independent of the AGP 9 block. It is not. See docs/TOOLCHAIN.md.
coreKtx = "1.15.0"
lifecycle = "2.8.7"
activityCompose = "1.9.3"
# foundation >= 1.9.0 needs AGP >= 8.8.2; stage 1 (AGP 8.13) cleared that floor. FlowRow and
# FlowColumn are stable from foundation 1.8.0, so no ExperimentalLayoutApi opt-in is needed.
composeBom = "2026.06.01"
navigationCompose = "2.8.5"
# 2.8.4: needs AGP >= 8.4 and minSdk >= 23 (we are on 8.13 and 26). room-ktx is emptied into
# room-runtime from 2.7.0 ("the artifact is now blank"), so it is no longer declared.
room = "2.8.4"
hilt = "2.52"
hiltNavigationCompose = "1.2.0"
biometric = "1.1.0"
securityCrypto = "1.1.0"
kotlinxSerialization = "1.7.3"
glance = "1.1.0"
qrcodegen = "1.8.0"
junit = "4.13.2"
coroutines = "1.9.0"
androidxTestExt = "1.2.1"
androidxTestCore = "1.6.1"
androidxTestRunner = "1.6.2"
# THE 5.2.0 EXPERIMENT — one CI run answers what cannot be answered on the dev machine.
#
# Why bother: lazysodium-android 5.1.0 bundles libsodium 1.0.18 (2019); 5.2.0 bundles 1.0.20
# (2024). That is the actual C cryptography on the phone, two releases behind, and the ONLY
# blocker recorded for moving is that 5.2.0's Gradle metadata marks it JVM-21-only while
# :sync-crypto sat on toolchain 17 next to :app. jvmToolchain is per-module, so :sync-crypto is
# raised to 21 alongside this bump; the open question — can AGP 8.13 / D8 consume a Java 21
# dependency for compileSdk 36, and can :app (target 17) compile against 21 classfiles — is
# exactly what this branch's CI run decides. dl.google.com does not resolve on the dev machine,
# so CI is the only oracle; do not merge this on a green feeling, merge it on a green run.
#
# The old comment here asserted that pinning both artifacts to one version guarantees API
# parity. That invariant was MEASURED FALSE at 5.1.0 (android exposes 328 public methods, java
# 259 — the gap is the whole Ristretto255 surface), so it is not restated. At 5.2.0 both carry
# ristretto; a reflection test should replace the prose either way (task #28).
lazysodium = "5.2.0"
jna = "5.17.0"

[libraries]
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-compose = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-compose", version.ref = "lifecycle" }
androidx-lifecycle-runtime-compose = { group = "androidx.lifecycle", name = "lifecycle-runtime-compose", version.ref = "lifecycle" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
androidx-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
androidx-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-material3 = { group = "androidx.compose.material3", name = "material3" }
androidx-material-icons-extended = { group = "androidx.compose.material", name = "material-icons-extended" }
androidx-navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigationCompose" }
androidx-room-runtime = { group = "androidx.room", name = "room-runtime", version.ref = "room" }
androidx-room-compiler = { group = "androidx.room", name = "room-compiler", version.ref = "room" }
hilt-android = { group = "com.google.dagger", name = "hilt-android", version.ref = "hilt" }
hilt-compiler = { group = "com.google.dagger", name = "hilt-android-compiler", version.ref = "hilt" }
androidx-hilt-navigation-compose = { group = "androidx.hilt", name = "hilt-navigation-compose", version.ref = "hiltNavigationCompose" }
androidx-biometric = { group = "androidx.biometric", name = "biometric", version.ref = "biometric" }
androidx-security-crypto = { group = "androidx.security", name = "security-crypto", version.ref = "securityCrypto" }
androidx-lifecycle-process = { group = "androidx.lifecycle", name = "lifecycle-process", version.ref = "lifecycle" }
kotlinx-serialization-json = { group = "org.jetbrains.kotlinx", name = "kotlinx-serialization-json", version.ref = "kotlinxSerialization" }
androidx-glance-appwidget = { group = "androidx.glance", name = "glance-appwidget", version.ref = "glance" }
nayuki-qrcodegen = { group = "io.nayuki", name = "qrcodegen", version.ref = "qrcodegen" }
kotlinx-coroutines-android = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-android", version.ref = "coroutines" }
junit = { group = "junit", name = "junit", version.ref = "junit" }
kotlinx-coroutines-test = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-test", version.ref = "coroutines" }
androidx-room-testing = { group = "androidx.room", name = "room-testing", version.ref = "room" }
androidx-test-ext-junit = { group = "androidx.test.ext", name = "junit", version.ref = "androidxTestExt" }
androidx-test-core = { group = "androidx.test", name = "core", version.ref = "androidxTestCore" }
androidx-test-runner = { group = "androidx.test", name = "runner", version.ref = "androidxTestRunner" }
lazysodium-android = { group = "com.goterl", name = "lazysodium-android", version.ref = "lazysodium" }
lazysodium-java = { group = "com.goterl", name = "lazysodium-java", version.ref = "lazysodium" }
jna = { group = "net.java.dev.jna", name = "jna", version.ref = "jna" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
hilt = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }
# Room's own Gradle plugin, so the exported-schema directory is a declared task
# input/output instead of a path KSP writes to blind. Same version as the Room libraries.
androidx-room = { id = "androidx.room", version.ref = "room" }
